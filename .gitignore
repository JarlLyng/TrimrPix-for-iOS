# Secrets
Secrets.xcconfig
Sentry.xcconfig
**/Secrets.swift

# Claude Code (local settings may contain tokens)
.claude/settings.local.json

# Xcode
*.xcuserstate
xcuserdata/
DerivedData/
*.xcscmblueprint

# macOS
.DS_Store

# SPM
.build/
.swiftpm/

# Build artifacts & distribution
*.ipa
*.xcarchive
build/

# Signing & provisioning (never commit these)
*.mobileprovision
*.p8
*.p12
*.pem
*.cer
*.certSigningRequest

# Environment files
.env
.env.*
!.env.example

# AI tooling: the repo is indifferent to which assistant is used and keeps one
# vendor-neutral AGENTS.md. Anything a specific editor or agent writes stays local.
# List from the hub's _template/.gitignore (PROJECT_STANDARDS §8).
.claude/
CLAUDE.md
CLAUDE.local.md
.cursor/
.cursorrules
.windsurfrules
.clinerules
.aider*
.continue/
.codex/
.mcp.json
GEMINI.md
.github/copilot-instructions.md
