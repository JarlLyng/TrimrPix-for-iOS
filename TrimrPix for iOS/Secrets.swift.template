// Secrets.swift — Copy this file to Secrets.swift and fill in your values
// Secrets.swift is gitignored and should never be committed

// Only the DSN is needed at runtime. The Sentry auth token / org / project
// belong to the dSYM-upload build phase and live in Sentry.xcconfig — never
// put them here, or they get compiled into the shipped app binary. See issue #29.
enum Secrets {
    static let sentryDSN = "YOUR_SENTRY_DSN_HERE"
}
